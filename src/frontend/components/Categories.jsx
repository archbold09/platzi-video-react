import React from 'react';
import '../assets/styles/components/Categories.scss';

const Categories = ({children}) => (
    <h4 className="categories__title"> {children} </h4>
)

export default Categories;